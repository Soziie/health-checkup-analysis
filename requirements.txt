# 건강검진 데이터 분석 실습 — 분석 환경
#
# 사용법 (Colab 노트북 첫 셀):
#   !pip install -r requirements.txt
#
# 버전은 재현성을 위해 고정(pinning)했습니다.
# 본인 Colab 환경에 맞추려면 아래로 현재 버전을 확인 후 수정하세요:
#   !pip freeze | grep -iE "pandas|numpy|matplotlib|seaborn|scipy|statsmodels|scikit"

# --- 데이터 처리 ---
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5          # pandas read_excel(.xlsx)에 필요

# --- 시각화 ---
matplotlib==3.10.0
seaborn==0.13.2

# --- 통계 검정 / 모델링 ---
scipy==1.13.1
statsmodels==0.14.4
scikit-learn==1.6.1
scikit-posthocs==0.11.2  # 사후검정(Dunn) — 노트북에서 직접 설치하는 패키지
